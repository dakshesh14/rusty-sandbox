services:
  rusty_sandbox:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: rusty_sandbox_dev
    privileged: true
    volumes:
      - .:/app
    working_dir: /app
    command: cargo run
    stdin_open: true
    tty: true
    mem_limit: 2g
    memswap_limit: 2g